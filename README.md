# BaiTu Project: End-to-End Enzyme Kinetics Prediction

This project implements a data-driven neural network for predicting enzyme reaction rates ($v_0$), trained on simulated kinetics data generated by aligning $k_{cat}$ and $K_m$ tables.

## Architecture

The model consists of two main modules:

1. **Encoder Layer**: Extracts features from enzyme sequences (ESM), substrate SMILES (Morgan Fingerprints), substrate concentration, and environmental conditions.
2. **Rate Regressor**: An MLP that directly predicts reaction rate $v_0$.

## Directory Structure

```text
BaiTu/
├── preprocess_esm.py   # ESM-2 embedding generation script (NEW)
├── model.py            # Core model definition
├── dataset.py          # Dataset loader (reads preprocessed .pt files)
├── train.py            # Training loop
├── config.py           # Configuration parameters
├── utils.py            # Utility functions
├── data/               # Data directory
│   ├── kinetics_data.csv               # Raw data
│   └── kinetics_data_with_embeddings.pt # Preprocessed data (generated by preprocess_esm.py)
├── checkpoints/        # Model checkpoints
├── requirements.txt    # Dependencies
└── README.md           # This file
```

## Setup

1. Install dependencies:

   ```bash
   pip install -r requirements.txt
   ```

2. **Generate simulated kinetics data from Km + kcat:**

   ```bash
   python data/generate_kinetics_from_km_kcat.py
   ```

3. **Preprocess Data (Crucial Step):**
   Before training, generate ESM-2 embeddings from the simulated table and save to `data/kinetics_simulated_with_embeddings.pt`.

   ```bash
   python preprocess_esm.py --csv_path data/kinetics_simulated_from_km_kcat.csv --output_path data/kinetics_simulated_with_embeddings.pt
   ```

## Usage

To train the model:

```bash
python train.py
```

## Key Features & Risk Mitigation

- **Synthetic Supervision**: Aligns `Km` and `kcat` to generate dense $v_0$ samples over substrate concentration grids.
- **Modality Balance**: Uses LayerNorm and projection layers to balance high-dimensional enzyme embeddings and sparse molecular fingerprints.
- **Direct Regression**: Removes the in-model physics layer and predicts $v_0$ directly from fused features.
- **Metadata Preserved**: Keeps EC / Organism / UniProtID for downstream active-learning analysis.
